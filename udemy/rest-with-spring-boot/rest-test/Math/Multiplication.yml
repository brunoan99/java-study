info:
  name: Multiplication
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/math/multiplication/-1.1/2,1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
