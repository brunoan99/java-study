info:
  name: Square Root
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/math/squareRoot/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
