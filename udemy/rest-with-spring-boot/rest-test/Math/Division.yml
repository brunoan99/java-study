info:
  name: Division
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/math/division/-1.1/2,1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
