info:
  name: Config En
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8888/greeting-service/en
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
