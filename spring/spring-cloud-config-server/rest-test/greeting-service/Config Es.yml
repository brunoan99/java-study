info:
  name: Config Es
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8888/greeting-service/es
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
