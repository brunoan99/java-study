info:
  name: Config Default
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8888/greeting-service/default
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
