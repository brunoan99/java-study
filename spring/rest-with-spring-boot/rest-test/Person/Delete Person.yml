info:
  name: Delete Person
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/person/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
