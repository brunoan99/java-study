info:
  name: Update Person
  type: http
  seq: 5

http:
  method: PUT
  url: "{{url}}/person/1"
  body:
    type: json
    data: |-
      {
        "firstName": "Name",
        "lastName": "Last Name",
        "address": "Any Address",
        "gender": "Male"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
