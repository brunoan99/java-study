info:
  name: Create Person
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/person"
  body:
    type: json
    data: |-
      {
        "firstName": "Name",
        "lastName": "Last Name",
        "address": "Any Address",
        "gender": "Male"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
