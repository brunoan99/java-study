info:
  name: Greeting with Name
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/greeting?name=Bruno"
  params:
    - name: name
      value: Bruno
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
