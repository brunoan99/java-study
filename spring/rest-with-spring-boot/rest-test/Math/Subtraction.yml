info:
  name: Subtraction
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/math/subtraction/-1.1/2,1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
